Download @types/node from npm to get the full node API

We don't have to support every function or even every option of every function (always throw an Error if something is unsupported) but for the functions and options we do support the behaviour should be identical to the Node.js standard library.