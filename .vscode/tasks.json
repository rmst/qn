{
	"version": "2.0.0",
	"tasks": [
		{
			"label": "jix install",
			"type": "shell",
			"command": "jix install --find-manifest -f ${file}",
			"group": {
				"kind": "build"
			}
		},
		{
			"label": "jix install --dry-run",
			"type": "shell",
			"command": "jix install --dry-run --find-manifest -f ${file}",
			"group": {
				"kind": "build"
			}
		},
		{
			"label": "jix uninstall",
			"type": "shell",
			"command": "jix uninstall -f ${file}",
			"group": {
				"kind": "build"
			}
		},
		{
			"label": "- jix run",
			"type": "shell",
			"command": "jix run --find-manifest -f ${file}",
			"group": {
				"kind": "build"
			}
		}
	]
}
